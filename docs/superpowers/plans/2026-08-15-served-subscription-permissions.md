# Nginx 订阅文件权限修复实施计划

1. 修改权限回归测试：内部文件保持 `0600`，Nginx 发布文件要求 `0644`，先确认旧实现失败。
2. 为 `write_base64_file` 增加默认值为 `0600` 的可选权限参数，并校验参数。
3. 仅在同步 `SERVED_SUB_FILE` 时传入 `0644`。
4. 更新 README 的权限说明，运行全部测试和 Bash 语法检查。
5. 在当前 VPS 备份并更新 `cfy`，修复现有发布文件权限，通过 Nginx 本机请求验证。
6. 提交、推送并同步本地主分支，清理隔离工作树。
